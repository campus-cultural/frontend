/**
 * The UI is designed for light mode only. On web, following the OS dark theme
 * breaks contrast (for example, date picker labels on white modals).
 */
export function useColorScheme() {
  return 'light';
}
